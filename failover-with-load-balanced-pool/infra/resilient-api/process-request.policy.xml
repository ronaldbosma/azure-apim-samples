<policies>
    <inbound>
        <base />

        <validate-content unspecified-content-type-action="prevent" max-size="102400"
                          size-exceeded-action="prevent">
            <content type="application/json" validate-as="json" action="prevent" />
        </validate-content>

        <set-backend-service backend-id="load-balanced-pool" />
        <rewrite-uri template="api/ProcessRequestFunction" />
    </inbound>

    <backend>
        <retry condition="@(context.Response.StatusCode >= 500)"
               count="3" interval="1" first-fast-retry="true">
            <set-variable name="attempt-count" value='@(context.Variables.GetValueOrDefault<int>("attempt-count", 0)+1)' />
            <forward-request buffer-request-body="true" />
        </retry>
    </backend>

    <outbound>
        <base />

        <set-header name="X-Attempt-Count" exists-action="override">
            <value>@(context.Variables.GetValueOrDefault<int>("attempt-count", 0).ToString())</value>
        </set-header>
    </outbound>

    <on-error>
        <base />
    </on-error>
</policies>