<policies>
    <inbound>
        <base />
    </inbound>

    <backend>
        <base />
    </backend>

    <outbound>
        <!-- No error handling, which means the default behaviour applies -->
        <base />
    </outbound>

    <on-error>
        <base />
    </on-error>
</policies>