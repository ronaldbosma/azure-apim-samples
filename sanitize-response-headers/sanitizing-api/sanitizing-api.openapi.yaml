openapi: 3.0.1
info:
  title: Sanitizing API
  description: API for testing different approaches to response header sanitization
  version: 1.0.0
paths:
  /no-sanitization:
    get:
      summary: No Sanitization
      description: Returns safe and unsafe headers without sanitization
      operationId: no-sanitization
      parameters:
        - name: numberOfSafeHeadersToReturnFromBackend
          in: query
          description: Number of safe headers to return from Backend API
          required: true
          schema:
            type: integer
        - name: numberOfUnsafeHeadersToReturnFromBackend
          in: query
          description: Number of unsafe headers to return from Backend API
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Successful response
  /sanitize-with-allowlist:
    get:
      summary: Allowlist Sanitization
      description: Returns headers after allowlist sanitization
      operationId: sanitize-with-allowlist
      parameters:
        - name: numberOfSafeHeadersToReturnFromBackend
          in: query
          description: Number of safe headers to return from Backend API
          required: true
          schema:
            type: integer
        - name: numberOfUnsafeHeadersToReturnFromBackend
          in: query
          description: Number of unsafe headers to return from Backend API
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Successful response
  /sanitize-with-blocklist:
    get:
      summary: Blocklist Sanitization
      description: Returns headers after blocklist sanitization
      operationId: sanitize-with-blocklist
      parameters:
        - name: numberOfSafeHeadersToReturnFromBackend
          in: query
          description: Number of safe headers to return from Backend API
          required: true
          schema:
            type: integer
        - name: numberOfUnsafeHeadersToReturnFromBackend
          in: query
          description: Number of unsafe headers to return from Backend API
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Successful response
  /sanitize-explicitly:
    get:
      summary: Explicit Sanitization
      description: Returns headers after explicit sanitization
      operationId: sanitize-explicitly
      parameters:
        - name: numberOfSafeHeadersToReturnFromBackend
          in: query
          description: Number of safe headers to return from Backend API
          required: true
          schema:
            type: integer
        - name: numberOfUnsafeHeadersToReturnFromBackend
          in: query
          description: Number of unsafe headers to return from Backend API
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Successful response
